<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-5">
      <div class="card shadow">
        <div class="card-body p-5">
          <h2 class="text-center mb-4">🗺️ Iniciar Sesión</h2>

          <%= form_for(resource, as: resource_name, url: user_session_path, html: { data: { turbo: false } }) do |f| %>
            <div class="mb-3">
              <%= f.label :email, "Correo Electrónico", class: "form-label" %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "tu@email.com" %>
            </div>

            <div class="mb-3">
              <%= f.label :password, "Contraseña", class: "form-label" %>
              <div class="input-group">
                <%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "········", id: "devise-login-password" %>
                <button class="btn btn-outline-secondary" type="button" onclick="togglePasswordVisibility('devise-login-password', 'devise-eye-icon')">
                  <span id="devise-eye-icon">👁️</span>
                </button>
              </div>
            </div>

            <% if devise_mapping.rememberable? %>
              <div class="mb-3 form-check">
                <%= f.check_box :remember_me, class: "form-check-input" %>
                <%= f.label :remember_me, "Recordarme", class: "form-check-label" %>
              </div>
            <% end %>

            <div class="d-grid mb-3">
              <%= f.submit "Iniciar Sesión", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

          <div class="text-center">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function togglePasswordVisibility(fieldId, iconId) {
    const passwordField = document.getElementById(fieldId)
    const eyeIcon = document.getElementById(iconId)
    if (passwordField.type === 'password') {
      passwordField.type = 'text'
      eyeIcon.textContent = '🙈'
    } else {
      passwordField.type = 'password'
      eyeIcon.textContent = '👁️'
    }
  }
</script>
